{
  "name": "scanner-app-backend",
  "version": "1.0.0",
  "description": "Backend for the scanner application",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "NODE_ENV=test jest --config jest.config.cjs --coverage",
    "test:watch": "NODE_ENV=test jest --config jest.config.cjs --watch",
    "test:ci": "NODE_ENV=test jest --config jest.config.cjs --ci --coverage"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "@babel/core": "^7.26.7",
    "@babel/plugin-transform-modules-commonjs": "^7.24.0",
    "@babel/preset-env": "^7.26.7",
    "@babel/register": "^7.25.9",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "node-fetch": "^3.3.2",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  }
}
